<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KotlinDemo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/color_primary_variant</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorSecondaryVariant">@color/color_secondary_variant</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.KotlinDemo.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.KotlinDemo.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.KotlinDemo.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!-- Splash screen theme -->
    <style name="Theme.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>

        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_variant</item>
        <item name="colorAccent">@color/color_primary_variant</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/color_primary_variant</item>
    </style>

    <style name="InputDetail">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">@dimen/detail_layout_margin_top</item>
        <item name="android:enabled">false</item>
        <item name="android:paddingStart">@dimen/detail_input_padding_start</item>
        <item name="android:paddingEnd">@dimen/detail_input_padding_end</item>
        <item name="boxCornerRadiusTopEnd">@dimen/detail_input_corner</item>
        <item name="boxCornerRadiusTopStart">@dimen/detail_input_corner</item>
    </style>

    <style name="DualInputDetail">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">@integer/detail_input_weight_dual</item>
        <item name="android:enabled">false</item>
        <item name="boxCornerRadiusTopEnd">@dimen/detail_input_corner</item>
        <item name="boxCornerRadiusTopStart">@dimen/detail_input_corner</item>
    </style>

    <style name="InputEditTextDetail">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <style name="ViewDetail">
        <item name="android:layout_width">@dimen/detail_input_image</item>
        <item name="android:layout_height">@dimen/detail_input_image</item>
        <item name="android:layout_marginTop">@dimen/detail_layout_margin_top</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:paddingStart">@dimen/detail_input_padding_image</item>
        <item name="android:paddingEnd">@dimen/detail_input_padding_image</item>
    </style>
</resources>